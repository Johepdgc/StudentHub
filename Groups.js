import React from 'react';
import { View } from 'react-native';

export default function Groups() {
    return (
        <View>
            {/* logica para mostrar los grupos */}
        </View>
    );
}